# Defaults for omnifunken initscript, sourced by /etc/init.d/omnifunken

# Change this to prevent omnifunken from being started as a system service
START_DAEMON=false
USER=omnifunken
GROUP=nogroup

AIRPLAY_NAME=Wohnzimmer
LATENCY=500
